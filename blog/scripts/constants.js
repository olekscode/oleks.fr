const blogRawUrl = 'https://raw.githubusercontent.com/olekscode/Blog/master';
const metadataFolder = blogRawUrl + '/metadata';

export const urls = Object.freeze({
  postsFolder: blogRawUrl + '/posts',
  postsMetadataFile: metadataFolder + '/posts.json'
});
