---
const { title, href, hrefLabel = 'View all', intro } = Astro.props;
---
<div class="section-heading">
  <div>
    <h2>{title}</h2>
    {intro && <p>{intro}</p>}
  </div>
  {href && <a class="section-link" href={href}>{hrefLabel}</a>}
</div>
